package lisa.maths.SetTheory.Functions.Examples

/**
 * A constant function `f : A -> B` is a function such that `f(x) = c`
 * for all `x ∈ A`, for some constant term `c`.
 *
 * TODO: Define it.
 */
object ConstantFunction {}
