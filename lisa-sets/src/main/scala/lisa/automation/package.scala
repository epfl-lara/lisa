package lisa.automation

/**
 * The superposition prover's tactic, re-exported so that it sits beside [[Tableau]] and [[Tautology]]: a proof
 * importing `lisa.automation.*` reaches it without naming the subpackage it is implemented in.
 */
export lisa.automation.superposition.Superpose
